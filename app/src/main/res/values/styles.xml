<resources>

    <style name="AppTheme" parent="@style/MaterialDrawerTheme">
        <item name="android:textColor">#000</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomTheme" parent="MaterialDrawerTheme.TranslucentStatus">
        <!-- CAB :D -->
        <item name="windowActionModeOverlay">true</item>
    </style>

    <style name="CustomActionTheme" parent="MaterialDrawerTheme.Light.ActionBar">

    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">@style/ButtonBar</item>
        <item name="metaButtonBarButtonStyle">@style/ButtonBarButton</item>
    </style>

    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <!--<item name="android:background">@android:drawable/bottom_bar</item>-->
    </style>

    <style name="ButtonBarButton" />

    <style name="ActionPreference" parent="CustomActionTheme">
        <item name="homeAsUpIndicator">@drawable/done_pref_btn</item>
        <item name="android:homeAsUpIndicator">@drawable/done_pref_btn</item>
    </style>

</resources>
